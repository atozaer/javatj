Pay클래스를작성한다.
1. 정수형으로 money를 작성한다.
2. 생성자함수로 돈을 입력받는다. 
3. pay()함수로 결재를 작성한다.
3.1. 음료와 주문한갯수를 매개변수로 입력받는다.
3.2. 만약 음료수제고가없을시(주문수량>재고)
3.3. 재고가 부족합니다.
3.4. 재고부족하지않으면 만약 입금한돈이 음료가격보다작으면 (초기돈<가격)
3.5. 잔액이부족합니다.출력하고
3.6. money -= (drink.getPrice() * orders); //음료가격과 주문한수량에서 돈을 뺀다.
3.7. 음료에서수량을 꺼내서 주문한주량을 빼서 음료에 다시 입력한다.
3.8. 주문하신음료 음료에서 음료이름을꺼내서 가 결제되었습니다.
3.9. 남은 잔액은 money원 남았습니다.
Admin클래스를 작성한다.
1. 비공개로 패스워드를 정수형으로 만든다.pw
2. Admin생성자함수에 패스워드를 입력받는다.
3. checkPW함수에 정수형으로 pw을 입력받는다.(관리자번호확인메서드)
입력값과 관리자 번호가 같다면 true를 리턴한다.
4.1. 만약패스워드와 입력받은 패스워드가 같다면 true를 
4.2. 같지않으면 false를 리턴한다.
5. plusCnt함수에 음료를 입력받고 정수형으로 음료갯수를 입력받는다.(재고추가메서드)
6. drink음료에서갯수를 꺼내와서 입력받은 갯수를 더해서 음료에 입력한다.
추가하고자 하는 drink객체와 더할 값을 인자로 받는다.
Drink클래스를 작성한다.
1. 비공개로 음료수이름 저장할변수 private String name;
2. 비공개로 음료재고를 저장할변수 
3. 비공개로 static int pkNum = 1001; pk저장값
4. 비공개로 int pk; 고유 PK값
5. 비공개로 int price; 음료수값을저장할변수
6. 생성자 함수로 음료이름과 음료재고와 음료수값을 입력받는다.
7. 음료이름할당하고 음료갯수입력하고 음료가격입력하고 각음료마다 고유 PK값부여후 pk=pkNum 스태틱변수를 고유pk값에 할당한다.
8. pkNum++ 1씩증가한다.
9. 음료이름을 가져가는 get함수를 작성한다.
10. 음료이름을 입력받는 set함수를 작성한다.
11. 음료수량을 가져가는 get함수를 작성한다.
12. 음료수량을 입력받는 set함수를 작성한다.
13. 고유번호를 가져가는 get함수를 작성한다. static함수로 작성한다.
14. 고유번호를 입력받는 set함수를 작성한다. static함수로 작성한다.
15. 고유번호를 getPk()함수작성
16. 고유번호를 setPk()함수작성
17. 가격을 가져가는 get함수작성
18. 가격을 입력받는 set함수작성

 


 

